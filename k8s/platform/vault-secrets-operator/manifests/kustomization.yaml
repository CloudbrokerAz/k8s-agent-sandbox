apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - 01-namespace.yaml
  # CRDs and operator are installed via Helm
  # These are custom resources for our deployment:
  - 02-vaultconnection.yaml
  - 03-vaultauth.yaml
  # Example - uncomment after configuring Vault secrets
  # - 04-vaultstaticsecret-example.yaml

labels:
  - pairs:
      app.kubernetes.io/name: vault-secrets-operator
      app.kubernetes.io/component: secrets-sync
