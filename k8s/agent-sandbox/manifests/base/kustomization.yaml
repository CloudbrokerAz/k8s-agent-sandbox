apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Default: Pre-built image with SSH support
resources:
  - namespace.yaml
  - service.yaml
  - sandbox.yaml

# Common labels
commonLabels:
  app.kubernetes.io/name: agent-sandbox
  app.kubernetes.io/part-of: agent-sandbox-platform
